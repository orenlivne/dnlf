# Data

`SiouxFalls/` — the SiouxFalls transportation network (TNTP format), the prototype's test instance
(24 nodes, 76 directed arcs). Tiny and committed for reproducibility.

For larger experiments over the SuiteSparse corpus, point `GRAPH_DATA` at the shared corpus
(see `/Users/oren/code/data`), which lives once and is not duplicated across repos.
